Portal
======

A link to an address served by a local test server that responds with a
redirect. The caller substitutes the running server's URL for the
placeholder below before writing this file into a portal's docs/ directory.

`redirected <__REDIRECT_URL__>`_
