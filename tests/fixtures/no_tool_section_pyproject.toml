# pyproject.toml without [tool] section at all

[project]
name = "test-project"
version = "1.0.0"
description = "Test project without tool section"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
