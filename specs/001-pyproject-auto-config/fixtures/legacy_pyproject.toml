# Legacy [tool.poetry] format only
# Purpose: Test that strict PEP 621 validation rejects legacy format

[tool.poetry]
name = "test-legacy-portal"
version = "0.5.0"
description = "Legacy Poetry format project"
authors = ["Legacy Author <legacy@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
